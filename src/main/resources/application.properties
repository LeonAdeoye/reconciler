# Application Configuration
spring.application.name=reconciler
server.port=8080

# Reconciliation Configuration
reconciliation.config.path=classpath:reconciliation-config.json

# Logging Configuration
logging.level.root=INFO
logging.level.com.reconciler=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n
logging.file.name=logs/reconciliation.log
logging.file.max-size=100MB
logging.file.max-history=30

# Jackson Configuration
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.default-property-inclusion=non_null

# Example connection properties (use environment variables or override in application-local.properties)
# couchbase.user=admin
# couchbase.password=password
# mongo.uri=mongodb://localhost:27017
# oracle.url=jdbc:oracle:thin:@localhost:1521:XE
# oracle.user=user
# oracle.password=password

